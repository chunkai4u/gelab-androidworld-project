# Formal runs

The formal evaluation has not started. See [issue #4](https://github.com/chunkai4u/gelab-androidworld-project/issues/4).

- **Before the first run**, freeze the code revision, model and prompt version, device settings, seeds and step budgets, and write them down here.
- **For each run**, add one folder named after the task and run number, and one row in [`../runs.csv`](../runs.csv) labelled `formal`.
- **Keep every outcome.** Failures and infrastructure errors stay in the log with their reason; nothing is rerun and silently replaced.
